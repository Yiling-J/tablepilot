version: 2

builds:
  - id: macos #build:macos
    goos: [darwin]
    goarch: [amd64, arm64]

  - id: linux #build:linux
    goos: [linux]
    goarch: [amd64, arm64]

  - id: windows #build:windows
    goos: [windows]
    goarch: [amd64, arm64]

archives:
  - id: linux-archive
    ids: [linux]
    formats: [tar.gz]
  - id: macos-archive
    ids: [macos]
    formats: [zip]
  - id: windows-archive
    ids: [windows]
    formats: [zip]
