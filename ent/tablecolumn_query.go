// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"tablepilot/ent/predicate"
	"tablepilot/ent/tablecolumn"
	"tablepilot/ent/tablemeta"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TableColumnQuery is the builder for querying TableColumn entities.
type TableColumnQuery struct {
	config
	ctx           *QueryContext
	order         []tablecolumn.OrderOption
	inters        []Interceptor
	predicates    []predicate.TableColumn
	withTablemeta *TableMetaQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TableColumnQuery builder.
func (tcq *TableColumnQuery) Where(ps ...predicate.TableColumn) *TableColumnQuery {
	tcq.predicates = append(tcq.predicates, ps...)
	return tcq
}

// Limit the number of records to be returned by this query.
func (tcq *TableColumnQuery) Limit(limit int) *TableColumnQuery {
	tcq.ctx.Limit = &limit
	return tcq
}

// Offset to start from.
func (tcq *TableColumnQuery) Offset(offset int) *TableColumnQuery {
	tcq.ctx.Offset = &offset
	return tcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tcq *TableColumnQuery) Unique(unique bool) *TableColumnQuery {
	tcq.ctx.Unique = &unique
	return tcq
}

// Order specifies how the records should be ordered.
func (tcq *TableColumnQuery) Order(o ...tablecolumn.OrderOption) *TableColumnQuery {
	tcq.order = append(tcq.order, o...)
	return tcq
}

// QueryTablemeta chains the current query on the "tablemeta" edge.
func (tcq *TableColumnQuery) QueryTablemeta() *TableMetaQuery {
	query := (&TableMetaClient{config: tcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tablecolumn.Table, tablecolumn.FieldID, selector),
			sqlgraph.To(tablemeta.Table, tablemeta.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, tablecolumn.TablemetaTable, tablecolumn.TablemetaColumn),
		)
		fromU = sqlgraph.SetNeighbors(tcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TableColumn entity from the query.
// Returns a *NotFoundError when no TableColumn was found.
func (tcq *TableColumnQuery) First(ctx context.Context) (*TableColumn, error) {
	nodes, err := tcq.Limit(1).All(setContextOp(ctx, tcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tablecolumn.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tcq *TableColumnQuery) FirstX(ctx context.Context) *TableColumn {
	node, err := tcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TableColumn ID from the query.
// Returns a *NotFoundError when no TableColumn ID was found.
func (tcq *TableColumnQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tcq.Limit(1).IDs(setContextOp(ctx, tcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tablecolumn.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tcq *TableColumnQuery) FirstIDX(ctx context.Context) int {
	id, err := tcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TableColumn entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TableColumn entity is found.
// Returns a *NotFoundError when no TableColumn entities are found.
func (tcq *TableColumnQuery) Only(ctx context.Context) (*TableColumn, error) {
	nodes, err := tcq.Limit(2).All(setContextOp(ctx, tcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tablecolumn.Label}
	default:
		return nil, &NotSingularError{tablecolumn.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tcq *TableColumnQuery) OnlyX(ctx context.Context) *TableColumn {
	node, err := tcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TableColumn ID in the query.
// Returns a *NotSingularError when more than one TableColumn ID is found.
// Returns a *NotFoundError when no entities are found.
func (tcq *TableColumnQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tcq.Limit(2).IDs(setContextOp(ctx, tcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tablecolumn.Label}
	default:
		err = &NotSingularError{tablecolumn.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tcq *TableColumnQuery) OnlyIDX(ctx context.Context) int {
	id, err := tcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TableColumns.
func (tcq *TableColumnQuery) All(ctx context.Context) ([]*TableColumn, error) {
	ctx = setContextOp(ctx, tcq.ctx, ent.OpQueryAll)
	if err := tcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TableColumn, *TableColumnQuery]()
	return withInterceptors[[]*TableColumn](ctx, tcq, qr, tcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tcq *TableColumnQuery) AllX(ctx context.Context) []*TableColumn {
	nodes, err := tcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TableColumn IDs.
func (tcq *TableColumnQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tcq.ctx.Unique == nil && tcq.path != nil {
		tcq.Unique(true)
	}
	ctx = setContextOp(ctx, tcq.ctx, ent.OpQueryIDs)
	if err = tcq.Select(tablecolumn.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tcq *TableColumnQuery) IDsX(ctx context.Context) []int {
	ids, err := tcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tcq *TableColumnQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tcq.ctx, ent.OpQueryCount)
	if err := tcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tcq, querierCount[*TableColumnQuery](), tcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tcq *TableColumnQuery) CountX(ctx context.Context) int {
	count, err := tcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tcq *TableColumnQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tcq.ctx, ent.OpQueryExist)
	switch _, err := tcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tcq *TableColumnQuery) ExistX(ctx context.Context) bool {
	exist, err := tcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TableColumnQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tcq *TableColumnQuery) Clone() *TableColumnQuery {
	if tcq == nil {
		return nil
	}
	return &TableColumnQuery{
		config:        tcq.config,
		ctx:           tcq.ctx.Clone(),
		order:         append([]tablecolumn.OrderOption{}, tcq.order...),
		inters:        append([]Interceptor{}, tcq.inters...),
		predicates:    append([]predicate.TableColumn{}, tcq.predicates...),
		withTablemeta: tcq.withTablemeta.Clone(),
		// clone intermediate query.
		sql:       tcq.sql.Clone(),
		path:      tcq.path,
		modifiers: append([]func(*sql.Selector){}, tcq.modifiers...),
	}
}

// WithTablemeta tells the query-builder to eager-load the nodes that are connected to
// the "tablemeta" edge. The optional arguments are used to configure the query builder of the edge.
func (tcq *TableColumnQuery) WithTablemeta(opts ...func(*TableMetaQuery)) *TableColumnQuery {
	query := (&TableMetaClient{config: tcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tcq.withTablemeta = query
	return tcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TableColumn.Query().
//		GroupBy(tablecolumn.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tcq *TableColumnQuery) GroupBy(field string, fields ...string) *TableColumnGroupBy {
	tcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TableColumnGroupBy{build: tcq}
	grbuild.flds = &tcq.ctx.Fields
	grbuild.label = tablecolumn.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TableColumn.Query().
//		Select(tablecolumn.FieldCreatedAt).
//		Scan(ctx, &v)
func (tcq *TableColumnQuery) Select(fields ...string) *TableColumnSelect {
	tcq.ctx.Fields = append(tcq.ctx.Fields, fields...)
	sbuild := &TableColumnSelect{TableColumnQuery: tcq}
	sbuild.label = tablecolumn.Label
	sbuild.flds, sbuild.scan = &tcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TableColumnSelect configured with the given aggregations.
func (tcq *TableColumnQuery) Aggregate(fns ...AggregateFunc) *TableColumnSelect {
	return tcq.Select().Aggregate(fns...)
}

func (tcq *TableColumnQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tcq); err != nil {
				return err
			}
		}
	}
	for _, f := range tcq.ctx.Fields {
		if !tablecolumn.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tcq.path != nil {
		prev, err := tcq.path(ctx)
		if err != nil {
			return err
		}
		tcq.sql = prev
	}
	return nil
}

func (tcq *TableColumnQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TableColumn, error) {
	var (
		nodes       = []*TableColumn{}
		_spec       = tcq.querySpec()
		loadedTypes = [1]bool{
			tcq.withTablemeta != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TableColumn).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TableColumn{config: tcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(tcq.modifiers) > 0 {
		_spec.Modifiers = tcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tcq.withTablemeta; query != nil {
		if err := tcq.loadTablemeta(ctx, query, nodes, nil,
			func(n *TableColumn, e *TableMeta) { n.Edges.Tablemeta = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tcq *TableColumnQuery) loadTablemeta(ctx context.Context, query *TableMetaQuery, nodes []*TableColumn, init func(*TableColumn), assign func(*TableColumn, *TableMeta)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TableColumn)
	for i := range nodes {
		fk := nodes[i].TableID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tablemeta.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "table_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tcq *TableColumnQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tcq.querySpec()
	if len(tcq.modifiers) > 0 {
		_spec.Modifiers = tcq.modifiers
	}
	_spec.Node.Columns = tcq.ctx.Fields
	if len(tcq.ctx.Fields) > 0 {
		_spec.Unique = tcq.ctx.Unique != nil && *tcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tcq.driver, _spec)
}

func (tcq *TableColumnQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tablecolumn.Table, tablecolumn.Columns, sqlgraph.NewFieldSpec(tablecolumn.FieldID, field.TypeInt))
	_spec.From = tcq.sql
	if unique := tcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tcq.path != nil {
		_spec.Unique = true
	}
	if fields := tcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tablecolumn.FieldID)
		for i := range fields {
			if fields[i] != tablecolumn.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if tcq.withTablemeta != nil {
			_spec.Node.AddColumnOnce(tablecolumn.FieldTableID)
		}
	}
	if ps := tcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tcq *TableColumnQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tcq.driver.Dialect())
	t1 := builder.Table(tablecolumn.Table)
	columns := tcq.ctx.Fields
	if len(columns) == 0 {
		columns = tablecolumn.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tcq.sql != nil {
		selector = tcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tcq.ctx.Unique != nil && *tcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range tcq.modifiers {
		m(selector)
	}
	for _, p := range tcq.predicates {
		p(selector)
	}
	for _, p := range tcq.order {
		p(selector)
	}
	if offset := tcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tcq *TableColumnQuery) ForUpdate(opts ...sql.LockOption) *TableColumnQuery {
	if tcq.driver.Dialect() == dialect.Postgres {
		tcq.Unique(false)
	}
	tcq.modifiers = append(tcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tcq *TableColumnQuery) ForShare(opts ...sql.LockOption) *TableColumnQuery {
	if tcq.driver.Dialect() == dialect.Postgres {
		tcq.Unique(false)
	}
	tcq.modifiers = append(tcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tcq *TableColumnQuery) Modify(modifiers ...func(s *sql.Selector)) *TableColumnSelect {
	tcq.modifiers = append(tcq.modifiers, modifiers...)
	return tcq.Select()
}

// TableColumnGroupBy is the group-by builder for TableColumn entities.
type TableColumnGroupBy struct {
	selector
	build *TableColumnQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tcgb *TableColumnGroupBy) Aggregate(fns ...AggregateFunc) *TableColumnGroupBy {
	tcgb.fns = append(tcgb.fns, fns...)
	return tcgb
}

// Scan applies the selector query and scans the result into the given value.
func (tcgb *TableColumnGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tcgb.build.ctx, ent.OpQueryGroupBy)
	if err := tcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TableColumnQuery, *TableColumnGroupBy](ctx, tcgb.build, tcgb, tcgb.build.inters, v)
}

func (tcgb *TableColumnGroupBy) sqlScan(ctx context.Context, root *TableColumnQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tcgb.fns))
	for _, fn := range tcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tcgb.flds)+len(tcgb.fns))
		for _, f := range *tcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TableColumnSelect is the builder for selecting fields of TableColumn entities.
type TableColumnSelect struct {
	*TableColumnQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tcs *TableColumnSelect) Aggregate(fns ...AggregateFunc) *TableColumnSelect {
	tcs.fns = append(tcs.fns, fns...)
	return tcs
}

// Scan applies the selector query and scans the result into the given value.
func (tcs *TableColumnSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tcs.ctx, ent.OpQuerySelect)
	if err := tcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TableColumnQuery, *TableColumnSelect](ctx, tcs.TableColumnQuery, tcs, tcs.inters, v)
}

func (tcs *TableColumnSelect) sqlScan(ctx context.Context, root *TableColumnQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tcs.fns))
	for _, fn := range tcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tcs *TableColumnSelect) Modify(modifiers ...func(s *sql.Selector)) *TableColumnSelect {
	tcs.modifiers = append(tcs.modifiers, modifiers...)
	return tcs
}
